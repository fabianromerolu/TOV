/* Definición de una función llamada testController
const userController = (req, res) => {
    // Esta función imprime un mensaje en la consola
    res.status(200).send("Estamos recibiendo una solicitud de usuario");
};

const postController = (req, res) => {
    res.status(200).send("Estamos recibiendo una solicitud post");
}

// Exportación de la función testController como parte de un objeto
module.exports = {
    userController,
    postController,
}
*/
